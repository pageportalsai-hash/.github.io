// ============================================================================
// FRONTIER ZERO PHASE C/D — WORLD IMMERSION + DESTRUCTION + AUDIO
// Interiors, environmental props, surface-aware impacts, breakable objects,
// vehicle damage, biome atmosphere, richer audio and death animation polish.
// ============================================================================
const phaseCDDesktop37=matchMedia('(pointer:fine)').matches;

// ---------- Interiors + world dressing ----------
const interiorRoots37=[],interiorCollision37=[],breakables37=[];
const worldDress37=new THREE.Group();scene.add(worldDress37);
const mats37={
  floor:new THREE.MeshStandardMaterial({color:0x5d574c,roughness:.92}),wall:new THREE.MeshStandardMaterial({color:0xa69f91,roughness:.91}),
  wood:new THREE.MeshStandardMaterial({color:0x6c5138,roughness:.86}),dark:new THREE.MeshStandardMaterial({color:0x303538,roughness:.66,metalness:.28}),
  steel:new THREE.MeshStandardMaterial({color:0x656c70,roughness:.48,metalness:.62}),cloth:new THREE.MeshStandardMaterial({color:0x59686b,roughness:.95}),
  light:new THREE.MeshStandardMaterial({color:0xffe3ad,emissive:0xffc86c,emissiveIntensity:1.15,roughness:.38}),
  glass:new THREE.MeshPhysicalMaterial({color:0x7fa7b8,transparent:true,opacity:.34,roughness:.07,clearcoat:.7,depthWrite:false}),
  crate:new THREE.MeshStandardMaterial({color:0x73522f,roughness:.88}),red:new THREE.MeshStandardMaterial({color:0x7f3d34,roughness:.84})
};
function m37(parent,geo,mat,x=0,y=0,z=0,rx=0,ry=0,rz=0,shadow=true){const o=new THREE.Mesh(geo,mat);o.position.set(x,y,z);o.rotation.set(rx,ry,rz);o.castShadow=shadow;o.receiveShadow=true;parent.add(o);return o;}
function addInteriorCollision37(root,x,z,w,d,owner=null){interiorCollision37.push({root,x,z,w,d,owner37:owner});}
function registerBreakable37(mesh,type='prop',hp=35){mesh.userData.breakable37={type,hp,max:hp,broken:false};mesh.userData.surface37=type==='glass'?'glass':type==='wood'?'wood':type==='metal'?'metal':'concrete';if(!blockers.includes(mesh))blockers.push(mesh);breakables37.push(mesh);return mesh;}
function crate37(g,x,z,s=1){const c=m37(g,new THREE.BoxGeometry(.82*s,.62*s,.72*s),mats37.crate,x,.34*s,z);m37(c,new THREE.BoxGeometry(.88*s,.07*s,.78*s),mats37.dark,0,.24*s,0,0,0,0,false);registerBreakable37(c,'wood',42);addInteriorCollision37(g,x,z,.86*s,.76*s,c);return c;}
function shelf37(g,x,z,rot=0){const s=new THREE.Group();s.position.set(x,.18,z);s.rotation.y=rot;g.add(s);for(const xx of [-.7,.7])m37(s,new THREE.BoxGeometry(.07,1.8,.48),mats37.steel,xx,.9,0);for(const y of [.1,.62,1.14,1.65])m37(s,new THREE.BoxGeometry(1.45,.07,.52),mats37.steel,0,y,0);return s;}
function table37(g,x,z){m37(g,new THREE.BoxGeometry(1.55,.12,.82),mats37.wood,x,.84,z);for(const sx of [-.64,.64])for(const sz of [-.28,.28])m37(g,new THREE.BoxGeometry(.09,.78,.09),mats37.dark,x+sx,.42,z+sz);addInteriorCollision37(g,x,z,1.62,.88);}
function sofa37(g,x,z){m37(g,new THREE.BoxGeometry(1.75,.48,.72),mats37.cloth,x,.38,z);m37(g,new THREE.BoxGeometry(1.78,.65,.22),mats37.cloth,x,.73,z+.25,-.10);addInteriorCollision37(g,x,z,1.8,.85);}
function counter37(g,x,z,w=2.4){m37(g,new THREE.BoxGeometry(w,.82,.56),mats37.wood,x,.43,z);m37(g,new THREE.BoxGeometry(w+.08,.08,.64),mats37.dark,x,.87,z);addInteriorCollision37(g,x,z,w+.1,.62);}
function makeInterior37(building,i){
  const b=building?.userData?.bounds33;if(!b)return;const root=new THREE.Group();root.name='interior37';root.userData.bounds37=b;building.add(root);
  m37(root,new THREE.BoxGeometry(Math.max(3,b.w-.55),.055,Math.max(3,b.d-.55)),mats37.floor,0,.12,0,0,0,0,false);
  const h=2.4,door=1.25,split=Math.min(b.d*.56,4.2),seg=Math.max(.6,(split-door)/2);
  m37(root,new THREE.BoxGeometry(.10,h,seg),mats37.wall,0,h/2+.12,-door/2-seg/2);m37(root,new THREE.BoxGeometry(.10,h,seg),mats37.wall,0,h/2+.12,door/2+seg/2);
  addInteriorCollision37(root,0,-door/2-seg/2,.16,seg);addInteriorCollision37(root,0,door/2+seg/2,.16,seg);
  if(i%3===0){sofa37(root,-b.w*.23,b.d*.12);table37(root,b.w*.22,b.d*.12);counter37(root,-b.w*.22,-b.d*.25,Math.min(2.5,b.w*.35));shelf37(root,b.w*.28,-b.d*.26,Math.PI/2);}
  else if(i%3===1){shelf37(root,-b.w*.25,-b.d*.17);shelf37(root,b.w*.25,-b.d*.17);counter37(root,0,b.d*.21,Math.min(3.2,b.w*.48));crate37(root,-b.w*.30,b.d*.31,.85);crate37(root,b.w*.31,b.d*.30,.72);const t=m37(root,new THREE.CylinderGeometry(.30,.34,1.05,12),mats37.steel,b.w*.34,.66,-b.d*.33);registerBreakable37(t,'metal',70);}
  else {counter37(root,0,-b.d*.10,Math.min(3,b.w*.46));shelf37(root,-b.w*.27,b.d*.24);shelf37(root,b.w*.27,b.d*.24);crate37(root,-b.w*.31,-b.d*.31,.72);}
  m37(root,new THREE.BoxGeometry(.88,.065,.32),mats37.light,0,2.55,0,0,0,0,false);
  const light=((phaseCDDesktop37&&i<9)||(!phaseCDDesktop37&&i<3))?new THREE.PointLight(0xffd39a,.60,10,2):null;if(light){light.position.set(0,2.35,0);root.add(light);}root.userData.light37=light;
  for(const x of [-b.w*.24,b.w*.24]){const pane=m37(root,new THREE.BoxGeometry(Math.min(1.25,b.w*.18),.82,.035),mats37.glass,x,1.62,-b.d/2-.23,0,0,0,false);registerBreakable37(pane,'glass',18);}
  interiorRoots37.push(root);
}
const interiorLimit37=phaseCDDesktop37?Math.min(26,rebuiltBuildings33.length):Math.min(12,rebuiltBuildings33.length);for(let i=0;i<interiorLimit37;i++)makeInterior37(rebuiltBuildings33[i],i);
const blockedPositionBeforeInterior37=blockedPosition;
blockedPosition=function(x,z){if(blockedPositionBeforeInterior37(x,z))return true;for(const c of interiorCollision37){if(!c.root?.visible||!c.root?.parent?.visible||c.owner37?.userData?.breakable37?.broken)continue;const p=c.root.localToWorld(new THREE.Vector3(c.x,0,c.z));if(Math.abs(x-p.x)<c.w/2+.18&&Math.abs(z-p.z)<c.d/2+.18)return true;}return false;};

function barrelWorld37(x,z,color=0x4f5b61){const g=new THREE.Group();g.position.set(x,groundY(x,z),z);worldDress37.add(g);const o=m37(g,new THREE.CylinderGeometry(.30,.32,.82,12),new THREE.MeshStandardMaterial({color,roughness:.62,metalness:.44}),0,.43,0);registerBreakable37(o,'metal',65);g.userData.collision37={w:.7,d:.7};return g;}
function dumpster37(x,z,rot=0){const g=new THREE.Group();g.position.set(x,groundY(x,z),z);g.rotation.y=rot;worldDress37.add(g);const o=m37(g,new THREE.BoxGeometry(1.55,.90,.92),new THREE.MeshStandardMaterial({color:0x40534b,roughness:.78,metalness:.25}),0,.52,0);registerBreakable37(o,'metal',95);m37(g,new THREE.BoxGeometry(1.62,.08,.97),mats37.dark,0,1.0,0);g.userData.collision37={w:1.65,d:1.05};return g;}
function powerPole37(x,z){const g=new THREE.Group();g.position.set(x,groundY(x,z),z);worldDress37.add(g);m37(g,new THREE.CylinderGeometry(.10,.14,5.8,8),mats37.wood,0,2.9,0);m37(g,new THREE.BoxGeometry(2.25,.12,.12),mats37.wood,0,5.25,0);g.userData.collision37={w:.38,d:.38};return g;}
for(let i=0;i<Math.min(22,rebuiltBuildings33.length);i++){const b=rebuiltBuildings33[i].userData.bounds33;if(i%3===0)dumpster37(b.x+b.w*.62,b.z+b.d*.43,i*.8);if(i%4===0){barrelWorld37(b.x-b.w*.60,b.z+b.d*.35);barrelWorld37(b.x-b.w*.53,b.z+b.d*.48,0x6e4334);}if(i%5===0)powerPole37(b.x+b.w*.72,b.z-b.d*.62);}

// ---------- Surface VFX / decals / destruction ----------
const fxMax37=phaseCDDesktop37?140:72,fxPos37=new Float32Array(fxMax37*3),fxCol37=new Float32Array(fxMax37*3),fxLife37=new Float32Array(fxMax37),fxVel37=Array.from({length:fxMax37},()=>new THREE.Vector3());
for(let i=0;i<fxMax37;i++)fxPos37[i*3+1]=-999;
const fxGeom37=new THREE.BufferGeometry();fxGeom37.setAttribute('position',new THREE.BufferAttribute(fxPos37,3));fxGeom37.setAttribute('color',new THREE.BufferAttribute(fxCol37,3));
const fxPoints37=new THREE.Points(fxGeom37,new THREE.PointsMaterial({size:phaseCDDesktop37?.075:.06,vertexColors:true,transparent:true,opacity:.92,depthWrite:false,sizeAttenuation:true}));scene.add(fxPoints37);let fxCursor37=0;
function spawnParticles37(point,color,count=8,power=1,normal=null){const c=new THREE.Color(color),n=normal?.clone?.().normalize?.()||new THREE.Vector3(0,1,0);for(let q=0;q<count;q++){const i=fxCursor37++%fxMax37,side=new THREE.Vector3(rand(-1,1),rand(-.2,1.3),rand(-1,1)).normalize();fxPos37[i*3]=point.x+n.x*.04;fxPos37[i*3+1]=point.y+n.y*.04;fxPos37[i*3+2]=point.z+n.z*.04;fxVel37[i].copy(n).multiplyScalar(rand(.35,1.2)*power).addScaledVector(side,rand(.25,1.1)*power);fxLife37[i]=rand(.20,.55);fxCol37[i*3]=c.r;fxCol37[i*3+1]=c.g;fxCol37[i*3+2]=c.b;}fxGeom37.attributes.position.needsUpdate=true;fxGeom37.attributes.color.needsUpdate=true;}
const decals37=[];for(let i=0;i<(phaseCDDesktop37?44:22);i++){const d=new THREE.Mesh(new THREE.CircleGeometry(.055,8),new THREE.MeshBasicMaterial({color:0x20201e,transparent:true,opacity:.5,depthWrite:false,polygonOffset:true,polygonOffsetFactor:-2,polygonOffsetUnits:-2}));d.visible=false;scene.add(d);decals37.push(d);}let decalCursor37=0;
function placeDecal37(hit){if(!hit?.face)return;const d=decals37[decalCursor37++%decals37.length],n=hit.face.normal.clone().applyMatrix3(new THREE.Matrix3().getNormalMatrix(hit.object.matrixWorld)).normalize();d.position.copy(hit.point).addScaledVector(n,.012);d.quaternion.setFromUnitVectors(new THREE.Vector3(0,0,1),n);d.visible=true;d.userData.until37=performance.now()+9000;}
function ancestorCar37(o){let p=o;for(let i=0;i<8&&p;i++,p=p.parent)if(p.userData?.car19)return p.userData.car19;return null;}
function surface37(hit){const o=hit?.object;if(!o)return'dirt';if(o.userData?.surface37)return o.userData.surface37;if(o.userData?.resource?.kind==='tree')return'wood';if(ancestorCar37(o))return'metal';const m=Array.isArray(o.material)?o.material[0]:o.material;if((m?.metalness??0)>.38)return'metal';return o===terrain?'dirt':'concrete';}
function breakObject37(o,hit){const b=o?.userData?.breakable37;if(!b||b.broken)return;b.hp-=b.type==='glass'?24:20;if(b.hp>0)return;b.broken=true;o.userData.destroyed37=true;o.visible=false;for(let i=blockers.length-1;i>=0;i--)if(blockers[i]===o)blockers.splice(i,1);spawnParticles37(hit?.point||o.getWorldPosition(new THREE.Vector3()),b.type==='glass'?0xb8e3f5:b.type==='wood'?0xa77b4e:0xffb34f,b.type==='glass'?14:9,1.3,hit?.face?.normal);soundImpact37(b.type,true);}
let lastRayHit37=null;const nearestHitBeforeImmersion37=nearestHit;nearestHit=function(dir,maxDist=130){const h=nearestHitBeforeImmersion37(dir,maxDist);lastRayHit37=h;return h;};
const impactBeforeImmersion37=impact;impact=function(point,color){const hit=lastRayHit37;lastRayHit37=null;impactBeforeImmersion37(point,color);if(!hit?.object)return;const s=surface37(hit),n=hit.face?.normal;spawnParticles37(point,s==='metal'?0xffc05c:s==='wood'?0xae7c49:s==='glass'?0xb8e3f5:s==='dirt'?0x8c7758:0xb7b0a5,phaseCDDesktop37?8:4,s==='metal'?1.35:.85,n);if(s!=='glass'&&s!=='dirt')placeDecal37(hit);breakObject37(hit.object,hit);soundImpact37(s,false);const car=ancestorCar37(hit.object);if(car)damageCar37(car,20,hit.point);};

const carHitMat37=new THREE.MeshBasicMaterial({transparent:true,opacity:0,depthWrite:false,colorWrite:false});
for(const c of cars19){c.hp37=260;c.maxHp37=260;c.wrecked37=false;const proxy=new THREE.Mesh(new THREE.BoxGeometry(3.35,1.75,5.6),carHitMat37);proxy.position.set(0,1.05,0);proxy.userData.carHit37=c;proxy.userData.surface37='metal';c.group.add(proxy);c.hitProxy37=proxy;if(!blockers.includes(proxy))blockers.push(proxy);}
const carSmoke37=[];function makeCarSmoke37(c){if(c.smoke37)return;const g=new THREE.Group();g.position.set(0,1.45,-1.3);c.group.add(g);c.smoke37=g;for(let i=0;i<5;i++){const s=m37(g,new THREE.SphereGeometry(.14+Math.random()*.10,7,6),new THREE.MeshBasicMaterial({color:0x4a4b49,transparent:true,opacity:.16,depthWrite:false}),rand(-.2,.2),rand(0,.3),rand(-.2,.2),0,0,0,false);s.userData.phase37=Math.random()*6.28;carSmoke37.push({car:c,s});}}
function damageCar37(c,amount,point){if(!c||c.wrecked37)return;c.hp37=Math.max(0,c.hp37-amount);if(c.hp37<150)makeCarSmoke37(c);if(c.hp37<=0){c.wrecked37=true;c.speed=0;c.maxSpeed=0;c.group.traverse(o=>{if(o.isMesh&&o.material?.color&&o!==c.hitProxy37){o.material=o.material.clone();o.material.color.multiplyScalar(.5);if('roughness'in o.material)o.material.roughness=Math.max(.72,o.material.roughness);}});spawnParticles37(point||c.group.position,0xff9a42,20,2.3,new THREE.Vector3(0,1,0));soundExplosion37();}}
const enterCarBeforeDamage37=enterCar19;enterCar19=function(c){if(c?.wrecked37){message('VEHICLE WRECKED',1.3);return;}enterCarBeforeDamage37(c);};

// ---------- Procedural sound ----------
let ambientSource37=null,ambientGain37=null,engineOsc37=null,engineGain37=null,lastImpactAudio37=0;
function ensureAudio37(){try{audioCtx||=new(window.AudioContext||window.webkitAudioContext)();if(audioCtx.state==='suspended')audioCtx.resume?.();if(!ambientSource37){const len=(audioCtx.sampleRate*.8)|0,buf=audioCtx.createBuffer(1,len,audioCtx.sampleRate),d=buf.getChannelData(0);for(let i=0;i<len;i++)d[i]=Math.random()*2-1;ambientSource37=audioCtx.createBufferSource();ambientSource37.buffer=buf;ambientSource37.loop=true;const f=audioCtx.createBiquadFilter();f.type='lowpass';f.frequency.value=450;ambientGain37=audioCtx.createGain();ambientGain37.gain.value=.003;ambientSource37.connect(f);f.connect(ambientGain37);ambientGain37.connect(audioCtx.destination);ambientSource37.start();}if(!engineOsc37){engineOsc37=audioCtx.createOscillator();engineOsc37.type='sawtooth';engineGain37=audioCtx.createGain();engineGain37.gain.value=.0001;const f=audioCtx.createBiquadFilter();f.type='lowpass';f.frequency.value=260;engineOsc37.connect(f);f.connect(engineGain37);engineGain37.connect(audioCtx.destination);engineOsc37.start();}return audioCtx;}catch(e){return null;}}
function noiseBurst37(dur=.04,vol=.02,hp=300){const ctx=ensureAudio37();if(!ctx)return;const len=Math.max(1,(ctx.sampleRate*dur)|0),buf=ctx.createBuffer(1,len,ctx.sampleRate),d=buf.getChannelData(0);for(let i=0;i<len;i++)d[i]=(Math.random()*2-1)*(1-i/len);const s=ctx.createBufferSource(),f=ctx.createBiquadFilter(),g=ctx.createGain();s.buffer=buf;f.type='highpass';f.frequency.value=hp;g.gain.value=vol;s.connect(f);f.connect(g);g.connect(ctx.destination);s.start();}
function thump37(freq=90,dur=.06,vol=.025){const ctx=ensureAudio37();if(!ctx)return;const o=ctx.createOscillator(),g=ctx.createGain();o.type='sine';o.frequency.setValueAtTime(freq,ctx.currentTime);o.frequency.exponentialRampToValueAtTime(Math.max(28,freq*.45),ctx.currentTime+dur);g.gain.setValueAtTime(vol,ctx.currentTime);g.gain.exponentialRampToValueAtTime(.0001,ctx.currentTime+dur);o.connect(g);g.connect(ctx.destination);o.start();o.stop(ctx.currentTime+dur+.01);}
function soundImpact37(type,broken=false){const now=performance.now();if(!broken&&now-lastImpactAudio37<40)return;lastImpactAudio37=now;if(type==='metal')noiseBurst37(.025,broken?.026:.012,1500);else if(type==='glass')noiseBurst37(.05,broken?.036:.017,2200);else if(type==='wood')noiseBurst37(.032,broken?.022:.011,700);else noiseBurst37(.022,.008,380);}
function soundExplosion37(){noiseBurst37(.20,.060,70);thump37(45,.26,.066);}
function gunSound37(w){const f=weaponFamily27(w);if(f==='shotgun'){noiseBurst37(.07,.048,120);thump37(72,.11,.042);}else if(f==='pistol'||f==='revolver'){noiseBurst37(.042,.030,500);thump37(115,.065,.026);}else if(f==='smg'){noiseBurst37(.025,.022,700);}else{noiseBurst37(.036,.027,430);thump37(f==='dmr'?88:102,.065,.023);}}
const shootGunBeforeAudio37=shootGun;shootGun=function(w){const before=w?.mag,r=shootGunBeforeAudio37(w);if(w?.type==='gun'&&before>w.mag)gunSound37(w);return r;};
const reloadBeforeAudio37=reload;reload=function(){const was=reloading;reloadBeforeAudio37();if(!was&&reloading){ensureAudio37();setTimeout(()=>noiseBurst37(.018,.009,1600),180);setTimeout(()=>noiseBurst37(.024,.012,1100),500);}};
let footPrev37=player.pos.clone(),footDist37=0;const updatePlayerBeforeSound37=updatePlayer;updatePlayer=function(dt){const ox=footPrev37.x,oz=footPrev37.z;updatePlayerBeforeSound37(dt);const moved=Math.hypot(player.pos.x-ox,player.pos.z-oz);footPrev37.copy(player.pos);if(!activeCar19&&!player.dead&&!(typeof lobbyActive17!=='undefined'&&lobbyActive17)){footDist37+=moved;if(footDist37>2.15){footDist37=0;noiseBurst37(.018,.0055,650);}}if(audioCtx&&engineOsc37&&engineGain37){const sp=activeCar19?Math.abs(activeCar19.speed||0):0;engineOsc37.frequency.setTargetAtTime(48+sp*5.6,audioCtx.currentTime,.06);const gain=activeCar19?.wrecked37?.0001;const target=activeCar19?.wrecked37?.0001:0;}};
// Correct engine update is separated to keep the expression unambiguous.
const updatePlayerBeforeEngine37=updatePlayer;updatePlayer=function(dt){updatePlayerBeforeEngine37(dt);if(audioCtx&&engineOsc37&&engineGain37){const sp=activeCar19?Math.abs(activeCar19.speed||0):0;const target=activeCar19?(activeCar19.wrecked37?.0001:Math.min(.022,.006+sp*.00055)):.0001;engineOsc37.frequency.setTargetAtTime(48+sp*5.6,audioCtx.currentTime,.06);engineGain37.gain.setTargetAtTime(target,audioCtx.currentTime,.08);}};

// ---------- Biome atmosphere ----------
const atmoCount37=phaseCDDesktop37?140:68,atmoPos37=new Float32Array(atmoCount37*3),atmoVel37=new Float32Array(atmoCount37*3);for(let i=0;i<atmoCount37;i++){atmoPos37[i*3]=rand(-28,28);atmoPos37[i*3+1]=rand(.2,12);atmoPos37[i*3+2]=rand(-28,28);atmoVel37[i*3]=rand(-.12,.12);atmoVel37[i*3+1]=rand(-.06,.04);atmoVel37[i*3+2]=rand(-.1,.1);}
const atmoGeo37=new THREE.BufferGeometry();atmoGeo37.setAttribute('position',new THREE.BufferAttribute(atmoPos37,3));const atmoMat37=new THREE.PointsMaterial({color:0xd9d2ba,size:.08,transparent:true,opacity:.2,depthWrite:false});const atmo37=new THREE.Points(atmoGeo37,atmoMat37);scene.add(atmo37);
const mist37=[];for(let i=0;i<(phaseCDDesktop37?8:4);i++){const o=new THREE.Mesh(new THREE.CircleGeometry(rand(4,8),18),new THREE.MeshBasicMaterial({color:0xb7c5c1,transparent:true,opacity:.035,depthWrite:false,side:THREE.DoubleSide}));o.rotation.x=-Math.PI/2;o.visible=false;o.userData.angle37=i/(phaseCDDesktop37?8:4)*Math.PI*2;o.userData.radius37=rand(7,18);scene.add(o);mist37.push(o);}
const sunGlare37=document.createElement('div');sunGlare37.style.cssText='position:fixed;inset:0;z-index:4;pointer-events:none;background:radial-gradient(circle at 17% 14%,rgba(255,226,172,.15),rgba(255,226,172,.04) 12%,transparent 28%)';document.body.appendChild(sunGlare37);
function updateAtmosphere37(dt){const b=typeof biomeAt==='function'?biomeAt(player.pos.x,player.pos.z):null,alpine=b===BIOMES.alpine,bad=b===BIOMES.badlands,marsh=b===BIOMES.marsh;atmo37.position.set(player.pos.x,groundY(player.pos.x,player.pos.z),player.pos.z);atmoMat37.color.setHex(alpine?0xf4f6f5:bad?0xcaa87b:marsh?0xd3ded9:0xd9d2ba);atmoMat37.opacity=alpine?.4:bad?.2:marsh?.15:.1;const p=atmoGeo37.attributes.position.array;for(let i=0;i<atmoCount37;i++){p[i*3]+=atmoVel37[i*3]*dt*4;p[i*3+1]+=(alpine?-.65:atmoVel37[i*3+1])*dt;p[i*3+2]+=atmoVel37[i*3+2]*dt*4;if(Math.abs(p[i*3])>30)p[i*3]*=-.95;if(Math.abs(p[i*3+2])>30)p[i*3+2]*=-.95;if(p[i*3+1]<.1)p[i*3+1]=rand(6,13);}atmoGeo37.attributes.position.needsUpdate=true;for(const m of mist37){m.visible=marsh;if(marsh){const a=m.userData.angle37+performance.now()*.000025,r=m.userData.radius37;m.position.set(player.pos.x+Math.cos(a)*r,groundY(player.pos.x,player.pos.z)+.20+Math.sin(a*2)*.1,player.pos.z+Math.sin(a)*r);}}}

// ---------- Death polish + update loop ----------
const killNpcBeforeDeathAnim37=killNpc;killNpc=function(n){const rec=n?.phaseBRec35,alive=!!(n&&!n.dead);killNpcBeforeDeathAnim37(n);if(alive&&rec){rec.lArm.rotation.z=-1.0;rec.rArm.rotation.z=1.0;rec.lLeg.rotation.x=.4;rec.rLeg.rotation.x=-.35;rec.head.rotation.z=rand(-.35,.35);spawnParticles37(n.group.position.clone().add(new THREE.Vector3(0,1.45,0)),0x8d4439,7,.45,new THREE.Vector3(0,1,0));}};
let interiorFrame37=0;const updateNPCsBeforeWorldFX37=updateNPCs;updateNPCs=function(dt){updateNPCsBeforeWorldFX37(dt);const p=fxGeom37.attributes.position.array;for(let i=0;i<fxMax37;i++)if(fxLife37[i]>0){fxLife37[i]-=dt;fxVel37[i].y-=4.8*dt;p[i*3]+=fxVel37[i].x*dt;p[i*3+1]+=fxVel37[i].y*dt;p[i*3+2]+=fxVel37[i].z*dt;if(fxLife37[i]<=0)p[i*3+1]=-999;}fxGeom37.attributes.position.needsUpdate=true;const now=performance.now();for(const d of decals37)if(d.visible&&now>(d.userData.until37||0))d.visible=false;for(const q of carSmoke37){const t=now*.001+q.s.userData.phase37;q.s.position.y=.15+((t*.28)%1.2);q.s.position.x=Math.sin(t*1.7)*.16;q.s.material.opacity=Math.max(0,.17*(1-(q.s.position.y-.15)/1.2));}updateAtmosphere37(dt);if((interiorFrame37++%12)===0){for(const root of interiorRoots37){const b=root.userData.bounds37,d=Math.hypot(b.x-player.pos.x,b.z-player.pos.z);root.visible=d<(phaseCDDesktop37?92:58);if(root.userData.light37)root.userData.light37.visible=d<34;}}};
message('WORLD IMMERSION COMPLETE · interiors, destruction, atmosphere, VFX + audio',4.2);
