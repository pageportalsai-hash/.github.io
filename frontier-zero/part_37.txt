// ============================================================================
// FRONTIER ZERO PHASE C/D — IMMERSION + DESTRUCTION + AUDIO
// ============================================================================
const phaseCDDesktop37=matchMedia('(pointer:fine)').matches;

// ---------- Interiors ----------
const interiorRoots37=[];
const interiorCollision37=[];
const breakables37=[];
const worldDress37=new THREE.Group();scene.add(worldDress37);
const floorMat37=new THREE.MeshStandardMaterial({color:0x5d574c,roughness:.92});
const wallMat37=new THREE.MeshStandardMaterial({color:0xa69f91,roughness:.91});
const woodMat37=new THREE.MeshStandardMaterial({color:0x6c5138,roughness:.86});
const darkMat37=new THREE.MeshStandardMaterial({color:0x303538,roughness:.66,metalness:.28});
const steelMat37=new THREE.MeshStandardMaterial({color:0x656c70,roughness:.48,metalness:.62});
const clothMat37=new THREE.MeshStandardMaterial({color:0x59686b,roughness:.95});
const lightMat37=new THREE.MeshStandardMaterial({color:0xffe3ad,emissive:0xffc86c,emissiveIntensity:1.15,roughness:.38});
const glassMat37=new THREE.MeshPhysicalMaterial({color:0x7fa7b8,transparent:true,opacity:.34,roughness:.07,clearcoat:.7,depthWrite:false});
const crateMat37=new THREE.MeshStandardMaterial({color:0x73522f,roughness:.88});
function mesh37(parent,geo,mat,x=0,y=0,z=0,rx=0,ry=0,rz=0,shadow=true){const m=new THREE.Mesh(geo,mat);m.position.set(x,y,z);m.rotation.set(rx,ry,rz);m.castShadow=shadow;m.receiveShadow=true;parent.add(m);return m;}
function addCollision37(root,x,z,w,d,owner=null){interiorCollision37.push({root,x,z,w,d,owner37:owner});}
function addBreakable37(mesh,type='prop',hp=35){mesh.userData.breakable37={type,hp,max:hp,broken:false};mesh.userData.surface37=type;if(!blockers.includes(mesh))blockers.push(mesh);breakables37.push(mesh);return mesh;}
function addCrate37(g,x,z,s=1){const c=mesh37(g,new THREE.BoxGeometry(.82*s,.62*s,.72*s),crateMat37,x,.34*s,z);mesh37(c,new THREE.BoxGeometry(.88*s,.07*s,.78*s),darkMat37,0,.24*s,0,0,0,0,false);addBreakable37(c,'wood',42);addCollision37(g,x,z,.86*s,.76*s,c);return c;}
function addShelf37(g,x,z,rot=0){const s=new THREE.Group();s.position.set(x,.18,z);s.rotation.y=rot;g.add(s);for(const xx of [-.7,.7])mesh37(s,new THREE.BoxGeometry(.07,1.8,.48),steelMat37,xx,.9,0);for(const y of [.1,.62,1.14,1.65])mesh37(s,new THREE.BoxGeometry(1.45,.07,.52),steelMat37,0,y,0);}
function addTable37(g,x,z){mesh37(g,new THREE.BoxGeometry(1.55,.12,.82),woodMat37,x,.84,z);for(const sx of [-.64,.64])for(const sz of [-.28,.28])mesh37(g,new THREE.BoxGeometry(.09,.78,.09),darkMat37,x+sx,.42,z+sz);addCollision37(g,x,z,1.62,.88);}
function addSofa37(g,x,z){mesh37(g,new THREE.BoxGeometry(1.75,.48,.72),clothMat37,x,.38,z);mesh37(g,new THREE.BoxGeometry(1.78,.65,.22),clothMat37,x,.73,z+.25,-.10);addCollision37(g,x,z,1.8,.85);}
function addCounter37(g,x,z,w){mesh37(g,new THREE.BoxGeometry(w,.82,.56),woodMat37,x,.43,z);mesh37(g,new THREE.BoxGeometry(w+.08,.08,.64),darkMat37,x,.87,z);addCollision37(g,x,z,w+.1,.62);}
function buildInterior37(building,i){
  const b=building&&building.userData?building.userData.bounds33:null;if(!b)return;
  const root=new THREE.Group();root.name='interior37';root.userData.bounds37=b;building.add(root);
  mesh37(root,new THREE.BoxGeometry(Math.max(3,b.w-.55),.055,Math.max(3,b.d-.55)),floorMat37,0,.12,0,0,0,0,false);
  const h=2.4,door=1.25,split=Math.min(b.d*.56,4.2),seg=Math.max(.6,(split-door)/2);
  mesh37(root,new THREE.BoxGeometry(.10,h,seg),wallMat37,0,h/2+.12,-door/2-seg/2);mesh37(root,new THREE.BoxGeometry(.10,h,seg),wallMat37,0,h/2+.12,door/2+seg/2);
  addCollision37(root,0,-door/2-seg/2,.16,seg);addCollision37(root,0,door/2+seg/2,.16,seg);
  if(i%3===0){addSofa37(root,-b.w*.23,b.d*.12);addTable37(root,b.w*.22,b.d*.12);addCounter37(root,-b.w*.22,-b.d*.25,Math.min(2.5,b.w*.35));addShelf37(root,b.w*.28,-b.d*.26,Math.PI/2);}
  else if(i%3===1){addShelf37(root,-b.w*.25,-b.d*.17);addShelf37(root,b.w*.25,-b.d*.17);addCounter37(root,0,b.d*.21,Math.min(3.2,b.w*.48));addCrate37(root,-b.w*.30,b.d*.31,.85);addCrate37(root,b.w*.31,b.d*.30,.72);const t=mesh37(root,new THREE.CylinderGeometry(.30,.34,1.05,12),steelMat37,b.w*.34,.66,-b.d*.33);addBreakable37(t,'metal',70);}
  else{addCounter37(root,0,-b.d*.10,Math.min(3,b.w*.46));addShelf37(root,-b.w*.27,b.d*.24);addShelf37(root,b.w*.27,b.d*.24);addCrate37(root,-b.w*.31,-b.d*.31,.72);}
  mesh37(root,new THREE.BoxGeometry(.88,.065,.32),lightMat37,0,2.55,0,0,0,0,false);
  let light=null;if((phaseCDDesktop37&&i<9)||(!phaseCDDesktop37&&i<3)){light=new THREE.PointLight(0xffd39a,.60,10,2);light.position.set(0,2.35,0);root.add(light);}root.userData.light37=light;
  for(const x of [-b.w*.24,b.w*.24]){const pane=mesh37(root,new THREE.BoxGeometry(Math.min(1.25,b.w*.18),.82,.035),glassMat37,x,1.62,-b.d/2-.23,0,0,0,false);addBreakable37(pane,'glass',18);}
  interiorRoots37.push(root);
}
const interiorLimit37=phaseCDDesktop37?Math.min(26,rebuiltBuildings33.length):Math.min(12,rebuiltBuildings33.length);for(let i=0;i<interiorLimit37;i++)buildInterior37(rebuiltBuildings33[i],i);
const blockedPositionBeforeInterior37=blockedPosition;
blockedPosition=function(x,z){if(blockedPositionBeforeInterior37(x,z))return true;for(const c of interiorCollision37){if(!c.root||!c.root.visible||!c.root.parent||!c.root.parent.visible)continue;if(c.owner37&&c.owner37.userData.breakable37&&c.owner37.userData.breakable37.broken)continue;const p=c.root.localToWorld(new THREE.Vector3(c.x,0,c.z));if(Math.abs(x-p.x)<c.w/2+.18&&Math.abs(z-p.z)<c.d/2+.18)return true;}return false;};

// ---------- Exterior props ----------
function barrelWorld37(x,z,color=0x4f5b61){const g=new THREE.Group();g.position.set(x,groundY(x,z),z);worldDress37.add(g);const b=mesh37(g,new THREE.CylinderGeometry(.30,.32,.82,12),new THREE.MeshStandardMaterial({color,roughness:.62,metalness:.44}),0,.43,0);addBreakable37(b,'metal',65);g.userData.collision37={w:.7,d:.7};return g;}
function dumpster37(x,z,rot=0){const g=new THREE.Group();g.position.set(x,groundY(x,z),z);g.rotation.y=rot;worldDress37.add(g);const b=mesh37(g,new THREE.BoxGeometry(1.55,.90,.92),new THREE.MeshStandardMaterial({color:0x40534b,roughness:.78,metalness:.25}),0,.52,0);addBreakable37(b,'metal',95);mesh37(g,new THREE.BoxGeometry(1.62,.08,.97),darkMat37,0,1,0);g.userData.collision37={w:1.65,d:1.05};return g;}
function powerPole37(x,z){const g=new THREE.Group();g.position.set(x,groundY(x,z),z);worldDress37.add(g);mesh37(g,new THREE.CylinderGeometry(.10,.14,5.8,8),woodMat37,0,2.9,0);mesh37(g,new THREE.BoxGeometry(2.25,.12,.12),woodMat37,0,5.25,0);g.userData.collision37={w:.38,d:.38};return g;}
for(let i=0;i<Math.min(22,rebuiltBuildings33.length);i++){const b=rebuiltBuildings33[i].userData.bounds33;if(i%3===0)dumpster37(b.x+b.w*.62,b.z+b.d*.43,i*.8);if(i%4===0){barrelWorld37(b.x-b.w*.60,b.z+b.d*.35);barrelWorld37(b.x-b.w*.53,b.z+b.d*.48,0x6e4334);}if(i%5===0)powerPole37(b.x+b.w*.72,b.z-b.d*.62);}

// ---------- Impact particles + decals ----------
const fxMax37=phaseCDDesktop37?140:72,fxPos37=new Float32Array(fxMax37*3),fxCol37=new Float32Array(fxMax37*3),fxLife37=new Float32Array(fxMax37),fxVel37=Array.from({length:fxMax37},()=>new THREE.Vector3());for(let i=0;i<fxMax37;i++)fxPos37[i*3+1]=-999;
const fxGeom37=new THREE.BufferGeometry();fxGeom37.setAttribute('position',new THREE.BufferAttribute(fxPos37,3));fxGeom37.setAttribute('color',new THREE.BufferAttribute(fxCol37,3));const fxPoints37=new THREE.Points(fxGeom37,new THREE.PointsMaterial({size:phaseCDDesktop37?.075:.06,vertexColors:true,transparent:true,opacity:.92,depthWrite:false}));scene.add(fxPoints37);let fxCursor37=0;
function spawnParticles37(point,color,count=8,power=1,normal=null){const c=new THREE.Color(color),n=normal?normal.clone().normalize():new THREE.Vector3(0,1,0);for(let q=0;q<count;q++){const i=fxCursor37++%fxMax37,side=new THREE.Vector3(rand(-1,1),rand(-.2,1.3),rand(-1,1)).normalize();fxPos37[i*3]=point.x+n.x*.04;fxPos37[i*3+1]=point.y+n.y*.04;fxPos37[i*3+2]=point.z+n.z*.04;fxVel37[i].copy(n).multiplyScalar(rand(.35,1.2)*power).addScaledVector(side,rand(.25,1.1)*power);fxLife37[i]=rand(.20,.55);fxCol37[i*3]=c.r;fxCol37[i*3+1]=c.g;fxCol37[i*3+2]=c.b;}fxGeom37.attributes.position.needsUpdate=true;fxGeom37.attributes.color.needsUpdate=true;}
const decals37=[];for(let i=0;i<(phaseCDDesktop37?44:22);i++){const d=new THREE.Mesh(new THREE.CircleGeometry(.055,8),new THREE.MeshBasicMaterial({color:0x20201e,transparent:true,opacity:.5,depthWrite:false,polygonOffset:true,polygonOffsetFactor:-2,polygonOffsetUnits:-2}));d.visible=false;scene.add(d);decals37.push(d);}let decalCursor37=0;
function placeDecal37(hit){if(!hit||!hit.face)return;const d=decals37[decalCursor37++%decals37.length],n=hit.face.normal.clone();n.applyMatrix3(new THREE.Matrix3().getNormalMatrix(hit.object.matrixWorld)).normalize();d.position.copy(hit.point).addScaledVector(n,.012);d.quaternion.setFromUnitVectors(new THREE.Vector3(0,0,1),n);d.visible=true;d.userData.until37=performance.now()+9000;}
function ancestorCar37(o){let p=o;for(let i=0;i<8&&p;i++,p=p.parent)if(p.userData&&p.userData.car19)return p.userData.car19;return null;}
function surface37(hit){const o=hit&&hit.object;if(!o)return'dirt';if(o.userData&&o.userData.surface37)return o.userData.surface37;if(o.userData&&o.userData.resource&&o.userData.resource.kind==='tree')return'wood';if(ancestorCar37(o))return'metal';const m=Array.isArray(o.material)?o.material[0]:o.material;if(m&&(m.metalness||0)>.38)return'metal';return o===terrain?'dirt':'concrete';}
function breakObject37(o,hit){const b=o&&o.userData?o.userData.breakable37:null;if(!b||b.broken)return;b.hp-=b.type==='glass'?24:20;if(b.hp>0)return;b.broken=true;o.userData.destroyed37=true;o.visible=false;for(let i=blockers.length-1;i>=0;i--)if(blockers[i]===o)blockers.splice(i,1);spawnParticles37(hit&&hit.point?hit.point:o.getWorldPosition(new THREE.Vector3()),b.type==='glass'?0xb8e3f5:b.type==='wood'?0xa77b4e:0xffb34f,b.type==='glass'?14:9,1.3,hit&&hit.face?hit.face.normal:null);soundImpact37(b.type,true);}
let lastRayHit37=null;const nearestHitBeforeImmersion37=nearestHit;nearestHit=function(dir,maxDist=130){const h=nearestHitBeforeImmersion37(dir,maxDist);lastRayHit37=h;return h;};
const impactBeforeImmersion37=impact;impact=function(point,color){const hit=lastRayHit37;lastRayHit37=null;impactBeforeImmersion37(point,color);if(!hit||!hit.object)return;const s=surface37(hit);let c=0xb7b0a5;if(s==='metal')c=0xffc05c;else if(s==='wood')c=0xae7c49;else if(s==='glass')c=0xb8e3f5;else if(s==='dirt')c=0x8c7758;spawnParticles37(point,c,phaseCDDesktop37?8:4,s==='metal'?1.35:.85,hit.face?hit.face.normal:null);if(s!=='glass'&&s!=='dirt')placeDecal37(hit);breakObject37(hit.object,hit);soundImpact37(s,false);const car=ancestorCar37(hit.object);if(car)damageCar37(car,20,hit.point);};

// ---------- Vehicle damage ----------
const carHitMat37=new THREE.MeshBasicMaterial({transparent:true,opacity:0,depthWrite:false,colorWrite:false});for(const c of cars19){c.hp37=260;c.maxHp37=260;c.wrecked37=false;const proxy=new THREE.Mesh(new THREE.BoxGeometry(3.35,1.75,5.6),carHitMat37);proxy.position.set(0,1.05,0);proxy.userData.carHit37=c;proxy.userData.surface37='metal';c.group.add(proxy);c.hitProxy37=proxy;if(!blockers.includes(proxy))blockers.push(proxy);}
const carSmoke37=[];function makeCarSmoke37(c){if(c.smoke37)return;const g=new THREE.Group();g.position.set(0,1.45,-1.3);c.group.add(g);c.smoke37=g;for(let i=0;i<5;i++){const s=mesh37(g,new THREE.SphereGeometry(.14+Math.random()*.1,7,6),new THREE.MeshBasicMaterial({color:0x4a4b49,transparent:true,opacity:.16,depthWrite:false}),rand(-.2,.2),rand(0,.3),rand(-.2,.2),0,0,0,false);s.userData.phase37=Math.random()*6.28;carSmoke37.push({car:c,s});}}
function damageCar37(c,amount,point){if(!c||c.wrecked37)return;c.hp37=Math.max(0,c.hp37-amount);if(c.hp37<150)makeCarSmoke37(c);if(c.hp37<=0){c.wrecked37=true;c.speed=0;c.maxSpeed=0;c.group.traverse(o=>{if(o.isMesh&&o.material&&o.material.color&&o!==c.hitProxy37){o.material=o.material.clone();o.material.color.multiplyScalar(.5);if('roughness'in o.material)o.material.roughness=Math.max(.72,o.material.roughness);}});spawnParticles37(point||c.group.position,0xff9a42,20,2.3,new THREE.Vector3(0,1,0));soundExplosion37();}}
const enterCarBeforeDamage37=enterCar19;enterCar19=function(c){if(c&&c.wrecked37){message('VEHICLE WRECKED',1.3);return;}enterCarBeforeDamage37(c);};

// ---------- Procedural audio ----------
let ambientSource37=null,ambientGain37=null,engineOsc37=null,engineGain37=null,lastImpactAudio37=0;
function ensureAudio37(){try{if(!audioCtx)audioCtx=new(window.AudioContext||window.webkitAudioContext)();if(audioCtx.state==='suspended'&&audioCtx.resume)audioCtx.resume();if(!ambientSource37){const len=(audioCtx.sampleRate*.8)|0,buf=audioCtx.createBuffer(1,len,audioCtx.sampleRate),d=buf.getChannelData(0);for(let i=0;i<len;i++)d[i]=Math.random()*2-1;ambientSource37=audioCtx.createBufferSource();ambientSource37.buffer=buf;ambientSource37.loop=true;const f=audioCtx.createBiquadFilter();f.type='lowpass';f.frequency.value=450;ambientGain37=audioCtx.createGain();ambientGain37.gain.value=.003;ambientSource37.connect(f);f.connect(ambientGain37);ambientGain37.connect(audioCtx.destination);ambientSource37.start();}if(!engineOsc37){engineOsc37=audioCtx.createOscillator();engineOsc37.type='sawtooth';engineGain37=audioCtx.createGain();engineGain37.gain.value=.0001;const f=audioCtx.createBiquadFilter();f.type='lowpass';f.frequency.value=260;engineOsc37.connect(f);f.connect(engineGain37);engineGain37.connect(audioCtx.destination);engineOsc37.start();}return audioCtx;}catch(e){return null;}}
function noiseBurst37(dur=.04,vol=.02,hp=300){const ctx=ensureAudio37();if(!ctx)return;const len=Math.max(1,(ctx.sampleRate*dur)|0),buf=ctx.createBuffer(1,len,ctx.sampleRate),d=buf.getChannelData(0);for(let i=0;i<len;i++)d[i]=(Math.random()*2-1)*(1-i/len);const s=ctx.createBufferSource(),f=ctx.createBiquadFilter(),g=ctx.createGain();s.buffer=buf;f.type='highpass';f.frequency.value=hp;g.gain.value=vol;s.connect(f);f.connect(g);g.connect(ctx.destination);s.start();}
function thump37(freq=90,dur=.06,vol=.025){const ctx=ensureAudio37();if(!ctx)return;const o=ctx.createOscillator(),g=ctx.createGain();o.type='sine';o.frequency.setValueAtTime(freq,ctx.currentTime);o.frequency.exponentialRampToValueAtTime(Math.max(28,freq*.45),ctx.currentTime+dur);g.gain.setValueAtTime(vol,ctx.currentTime);g.gain.exponentialRampToValueAtTime(.0001,ctx.currentTime+dur);o.connect(g);g.connect(ctx.destination);o.start();o.stop(ctx.currentTime+dur+.01);}
function soundImpact37(type,broken=false){const now=performance.now();if(!broken&&now-lastImpactAudio37<40)return;lastImpactAudio37=now;if(type==='metal')noiseBurst37(.025,broken?.026:.012,1500);else if(type==='glass')noiseBurst37(.05,broken?.036:.017,2200);else if(type==='wood')noiseBurst37(.032,broken?.022:.011,700);else noiseBurst37(.022,.008,380);}
function soundExplosion37(){noiseBurst37(.2,.06,70);thump37(45,.26,.066);}
function gunSound37(w){const f=weaponFamily27(w);if(f==='shotgun'){noiseBurst37(.07,.048,120);thump37(72,.11,.042);}else if(f==='pistol'||f==='revolver'){noiseBurst37(.042,.03,500);thump37(115,.065,.026);}else if(f==='smg')noiseBurst37(.025,.022,700);else{noiseBurst37(.036,.027,430);thump37(f==='dmr'?88:102,.065,.023);}}
const shootGunBeforeAudio37=shootGun;shootGun=function(w){const before=w?w.mag:null;const r=shootGunBeforeAudio37(w);if(w&&w.type==='gun'&&before>w.mag)gunSound37(w);return r;};
const reloadBeforeAudio37=reload;reload=function(){const was=reloading;reloadBeforeAudio37();if(!was&&reloading){ensureAudio37();setTimeout(()=>noiseBurst37(.018,.009,1600),180);setTimeout(()=>noiseBurst37(.024,.012,1100),500);}};
let footPrev37=player.pos.clone(),footDist37=0;const updatePlayerBeforeSound37=updatePlayer;updatePlayer=function(dt){const ox=footPrev37.x,oz=footPrev37.z;updatePlayerBeforeSound37(dt);const moved=Math.hypot(player.pos.x-ox,player.pos.z-oz);footPrev37.copy(player.pos);if(!activeCar19&&!player.dead&&!(typeof lobbyActive17!=='undefined'&&lobbyActive17)){footDist37+=moved;if(footDist37>2.15){footDist37=0;noiseBurst37(.018,.0055,650);}}if(audioCtx&&engineOsc37&&engineGain37){const sp=activeCar19?Math.abs(activeCar19.speed||0):0;let target=.0001;if(activeCar19&&!activeCar19.wrecked37)target=Math.min(.022,.006+sp*.00055);engineOsc37.frequency.setTargetAtTime(48+sp*5.6,audioCtx.currentTime,.06);engineGain37.gain.setTargetAtTime(target,audioCtx.currentTime,.08);}};

// ---------- Atmosphere ----------
const atmoCount37=phaseCDDesktop37?140:68,atmoPos37=new Float32Array(atmoCount37*3),atmoVel37=new Float32Array(atmoCount37*3);for(let i=0;i<atmoCount37;i++){atmoPos37[i*3]=rand(-28,28);atmoPos37[i*3+1]=rand(.2,12);atmoPos37[i*3+2]=rand(-28,28);atmoVel37[i*3]=rand(-.12,.12);atmoVel37[i*3+1]=rand(-.06,.04);atmoVel37[i*3+2]=rand(-.1,.1);}
const atmoGeo37=new THREE.BufferGeometry();atmoGeo37.setAttribute('position',new THREE.BufferAttribute(atmoPos37,3));const atmoMat37=new THREE.PointsMaterial({color:0xd9d2ba,size:.08,transparent:true,opacity:.2,depthWrite:false});const atmo37=new THREE.Points(atmoGeo37,atmoMat37);scene.add(atmo37);
const mist37=[];const mistCount37=phaseCDDesktop37?8:4;for(let i=0;i<mistCount37;i++){const o=new THREE.Mesh(new THREE.CircleGeometry(rand(4,8),18),new THREE.MeshBasicMaterial({color:0xb7c5c1,transparent:true,opacity:.035,depthWrite:false,side:THREE.DoubleSide}));o.rotation.x=-Math.PI/2;o.visible=false;o.userData.angle37=i/mistCount37*Math.PI*2;o.userData.radius37=rand(7,18);scene.add(o);mist37.push(o);}
const sunGlare37=document.createElement('div');sunGlare37.style.cssText='position:fixed;inset:0;z-index:4;pointer-events:none;background:radial-gradient(circle at 17% 14%,rgba(255,226,172,.15),rgba(255,226,172,.04) 12%,transparent 28%)';document.body.appendChild(sunGlare37);
function updateAtmosphere37(dt){const b=biomeAt(player.pos.x,player.pos.z);const alpine=b===BIOMES.alpine,bad=b===BIOMES.badlands,marsh=b===BIOMES.marsh;atmo37.position.set(player.pos.x,groundY(player.pos.x,player.pos.z),player.pos.z);atmoMat37.color.setHex(alpine?0xf4f6f5:bad?0xcaa87b:marsh?0xd3ded9:0xd9d2ba);atmoMat37.opacity=alpine?.4:bad?.2:marsh?.15:.1;const p=atmoGeo37.attributes.position.array;for(let i=0;i<atmoCount37;i++){p[i*3]+=atmoVel37[i*3]*dt*4;p[i*3+1]+=(alpine?-.65:atmoVel37[i*3+1])*dt;p[i*3+2]+=atmoVel37[i*3+2]*dt*4;if(Math.abs(p[i*3])>30)p[i*3]*=-.95;if(Math.abs(p[i*3+2])>30)p[i*3+2]*=-.95;if(p[i*3+1]<.1)p[i*3+1]=rand(6,13);}atmoGeo37.attributes.position.needsUpdate=true;for(const m of mist37){m.visible=marsh;if(marsh){const a=m.userData.angle37+performance.now()*.000025,r=m.userData.radius37;m.position.set(player.pos.x+Math.cos(a)*r,groundY(player.pos.x,player.pos.z)+.2+Math.sin(a*2)*.1,player.pos.z+Math.sin(a)*r);}}}

// ---------- Death + update loop ----------
const killNpcBeforeDeath37=killNpc;killNpc=function(n){const rec=n?n.phaseBRec35:null,alive=!!(n&&!n.dead);killNpcBeforeDeath37(n);if(alive&&rec){rec.lArm.rotation.z=-1;rec.rArm.rotation.z=1;rec.lLeg.rotation.x=.4;rec.rLeg.rotation.x=-.35;rec.head.rotation.z=rand(-.35,.35);spawnParticles37(n.group.position.clone().add(new THREE.Vector3(0,1.45,0)),0x8d4439,7,.45,new THREE.Vector3(0,1,0));}};
let interiorFrame37=0;const updateNPCsBeforeWorldFX37=updateNPCs;updateNPCs=function(dt){updateNPCsBeforeWorldFX37(dt);const p=fxGeom37.attributes.position.array;for(let i=0;i<fxMax37;i++)if(fxLife37[i]>0){fxLife37[i]-=dt;fxVel37[i].y-=4.8*dt;p[i*3]+=fxVel37[i].x*dt;p[i*3+1]+=fxVel37[i].y*dt;p[i*3+2]+=fxVel37[i].z*dt;if(fxLife37[i]<=0)p[i*3+1]=-999;}fxGeom37.attributes.position.needsUpdate=true;const now=performance.now();for(const d of decals37)if(d.visible&&now>(d.userData.until37||0))d.visible=false;for(const q of carSmoke37){const t=now*.001+q.s.userData.phase37;q.s.position.y=.15+((t*.28)%1.2);q.s.position.x=Math.sin(t*1.7)*.16;q.s.material.opacity=Math.max(0,.17*(1-(q.s.position.y-.15)/1.2));}updateAtmosphere37(dt);if((interiorFrame37++%12)===0){for(const root of interiorRoots37){const b=root.userData.bounds37,d=Math.hypot(b.x-player.pos.x,b.z-player.pos.z);root.visible=d<(phaseCDDesktop37?92:58);if(root.userData.light37)root.userData.light37.visible=d<34;}}};
message('WORLD IMMERSION COMPLETE · interiors, destruction, atmosphere, VFX + audio',4.2);
