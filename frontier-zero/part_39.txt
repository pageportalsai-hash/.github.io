// ============================================================================
// FRONTIER ZERO PHASE C-E CONSOLIDATED — IMMERSION + DESTRUCTION + AUDIO + LOD
// ============================================================================
const phaseAllDesktop39=matchMedia('(pointer:fine)').matches;
let fxBudget39=phaseAllDesktop39?1:0.58;

// ---------- Shared helpers ----------
function mesh39(parent,geo,mat,x,y,z,shadow){const m=new THREE.Mesh(geo,mat);m.position.set(x||0,y||0,z||0);m.castShadow=shadow!==false;m.receiveShadow=true;parent.add(m);return m;}
function visible39(o){let p=o;while(p){if(p.visible===false)return false;p=p.parent;}return true;}
function syncBlocker39(o,on){const i=blockers.indexOf(o);if(on&&i<0)blockers.push(o);if(!on&&i>=0)blockers.splice(i,1);}

// ---------- Authored interiors ----------
const interiors39=[];
const interiorCols39=[];
const breakables39=[];
const interiorFloor39=new THREE.MeshStandardMaterial({color:0x5b554a,roughness:.93});
const interiorWall39=new THREE.MeshStandardMaterial({color:0xa9a092,roughness:.92});
const interiorWood39=new THREE.MeshStandardMaterial({color:0x6a5038,roughness:.87});
const interiorSteel39=new THREE.MeshStandardMaterial({color:0x5d6569,roughness:.50,metalness:.60});
const interiorDark39=new THREE.MeshStandardMaterial({color:0x2d3235,roughness:.67,metalness:.25});
const interiorCloth39=new THREE.MeshStandardMaterial({color:0x59696c,roughness:.96});
const interiorGlow39=new THREE.MeshStandardMaterial({color:0xffdfaa,emissive:0xffc36a,emissiveIntensity:1.1,roughness:.4});
const glass39=new THREE.MeshPhysicalMaterial({color:0x8db5c6,transparent:true,opacity:.34,roughness:.06,clearcoat:.72,depthWrite:false});
function addInteriorCol39(root,x,z,w,d,owner){const p=root.localToWorld(new THREE.Vector3(x,0,z));interiorCols39.push({root:root,x:p.x,z:p.z,w:w,d:d,owner:owner||null});}
function breakable39(mesh,type,hp){mesh.userData.break39={type:type,hp:hp,broken:false};mesh.userData.surface39=type;breakables39.push(mesh);syncBlocker39(mesh,true);return mesh;}
function crate39(root,x,z,s){const sc=s||1,c=mesh39(root,new THREE.BoxGeometry(.82*sc,.62*sc,.72*sc),interiorWood39,x,.34*sc,z);mesh39(c,new THREE.BoxGeometry(.88*sc,.07*sc,.78*sc),interiorDark39,0,.24*sc,0,false);breakable39(c,'wood',42);addInteriorCol39(root,x,z,.86*sc,.76*sc,c);}
function shelf39(root,x,z,rot){const g=new THREE.Group();g.position.set(x,.18,z);g.rotation.y=rot||0;root.add(g);for(const xx of [-.7,.7])mesh39(g,new THREE.BoxGeometry(.07,1.8,.48),interiorSteel39,xx,.9,0);for(const y of [.1,.62,1.14,1.65])mesh39(g,new THREE.BoxGeometry(1.45,.07,.52),interiorSteel39,0,y,0);}
function table39(root,x,z){mesh39(root,new THREE.BoxGeometry(1.5,.12,.8),interiorWood39,x,.84,z);for(const sx of [-.62,.62])for(const sz of [-.27,.27])mesh39(root,new THREE.BoxGeometry(.09,.78,.09),interiorDark39,x+sx,.42,z+sz);addInteriorCol39(root,x,z,1.58,.86,null);}
function sofa39(root,x,z){mesh39(root,new THREE.BoxGeometry(1.72,.47,.72),interiorCloth39,x,.38,z);mesh39(root,new THREE.BoxGeometry(1.75,.64,.22),interiorCloth39,x,.72,z+.25);addInteriorCol39(root,x,z,1.78,.84,null);}
function counter39(root,x,z,w){mesh39(root,new THREE.BoxGeometry(w,.82,.56),interiorWood39,x,.43,z);mesh39(root,new THREE.BoxGeometry(w+.08,.08,.64),interiorDark39,x,.87,z);addInteriorCol39(root,x,z,w+.1,.62,null);}
function buildInterior39(building,i){
  const b=building&&building.userData?building.userData.bounds33:null;if(!b)return;
  const root=new THREE.Group();root.name='phaseInterior39';root.userData.bounds39=b;building.add(root);
  mesh39(root,new THREE.BoxGeometry(Math.max(3,b.w-.55),.055,Math.max(3,b.d-.55)),interiorFloor39,0,.12,0,false);
  const h=2.4,door=1.25,split=Math.min(b.d*.56,4.2),seg=Math.max(.6,(split-door)/2);
  mesh39(root,new THREE.BoxGeometry(.10,h,seg),interiorWall39,0,h/2+.12,-door/2-seg/2);mesh39(root,new THREE.BoxGeometry(.10,h,seg),interiorWall39,0,h/2+.12,door/2+seg/2);
  addInteriorCol39(root,0,-door/2-seg/2,.16,seg,null);addInteriorCol39(root,0,door/2+seg/2,.16,seg,null);
  if(i%3===0){sofa39(root,-b.w*.23,b.d*.12);table39(root,b.w*.22,b.d*.12);counter39(root,-b.w*.22,-b.d*.25,Math.min(2.5,b.w*.35));shelf39(root,b.w*.28,-b.d*.26,Math.PI/2);}
  if(i%3===1){shelf39(root,-b.w*.25,-b.d*.17,0);shelf39(root,b.w*.25,-b.d*.17,0);counter39(root,0,b.d*.21,Math.min(3.2,b.w*.48));crate39(root,-b.w*.30,b.d*.31,.85);crate39(root,b.w*.31,b.d*.30,.72);const tank=mesh39(root,new THREE.CylinderGeometry(.30,.34,1.05,12),interiorSteel39,b.w*.34,.66,-b.d*.33);breakable39(tank,'metal',70);}
  if(i%3===2){counter39(root,0,-b.d*.10,Math.min(3,b.w*.46));shelf39(root,-b.w*.27,b.d*.24,0);shelf39(root,b.w*.27,b.d*.24,0);crate39(root,-b.w*.31,-b.d*.31,.72);}
  mesh39(root,new THREE.BoxGeometry(.88,.065,.32),interiorGlow39,0,2.55,0,false);
  let light=null;if((phaseAllDesktop39&&i<8)||(!phaseAllDesktop39&&i<3)){light=new THREE.PointLight(0xffd29a,.58,9.5,2);light.position.set(0,2.35,0);root.add(light);}root.userData.light39=light;
  for(const wx of [-b.w*.24,b.w*.24]){const pane=mesh39(root,new THREE.BoxGeometry(Math.min(1.25,b.w*.18),.82,.035),glass39,wx,1.62,-b.d/2-.23,false);breakable39(pane,'glass',18);}
  interiors39.push(root);
}
const interiorCount39=phaseAllDesktop39?Math.min(24,rebuiltBuildings33.length):Math.min(11,rebuiltBuildings33.length);for(let i=0;i<interiorCount39;i++)buildInterior39(rebuiltBuildings33[i],i);

// ---------- Exterior props + physical collision ----------
const dress39=new THREE.Group();scene.add(dress39);const propCols39=[];
function propCol39(g,w,d){propCols39.push({g:g,w:w,d:d});}
function barrel39(x,z,color){const g=new THREE.Group();g.position.set(x,groundY(x,z),z);dress39.add(g);const m=mesh39(g,new THREE.CylinderGeometry(.3,.32,.82,12),new THREE.MeshStandardMaterial({color:color||0x4f5b61,roughness:.62,metalness:.44}),0,.43,0);breakable39(m,'metal',60);propCol39(g,.7,.7);return g;}
function dumpster39(x,z,rot){const g=new THREE.Group();g.position.set(x,groundY(x,z),z);g.rotation.y=rot||0;dress39.add(g);const m=mesh39(g,new THREE.BoxGeometry(1.55,.9,.92),new THREE.MeshStandardMaterial({color:0x40534b,roughness:.78,metalness:.25}),0,.52,0);breakable39(m,'metal',90);mesh39(g,new THREE.BoxGeometry(1.62,.08,.97),interiorDark39,0,1,0);propCol39(g,1.65,1.05);return g;}
function pole39(x,z){const g=new THREE.Group();g.position.set(x,groundY(x,z),z);dress39.add(g);mesh39(g,new THREE.CylinderGeometry(.1,.14,5.8,8),interiorWood39,0,2.9,0);mesh39(g,new THREE.BoxGeometry(2.2,.12,.12),interiorWood39,0,5.25,0);propCol39(g,.38,.38);return g;}
for(let i=0;i<Math.min(20,rebuiltBuildings33.length);i++){const b=rebuiltBuildings33[i].userData.bounds33;if(i%3===0)dumpster39(b.x+b.w*.62,b.z+b.d*.43,i*.8);if(i%4===0){barrel39(b.x-b.w*.60,b.z+b.d*.35,0x4f5b61);barrel39(b.x-b.w*.53,b.z+b.d*.48,0x6e4334);}if(i%5===0)pole39(b.x+b.w*.72,b.z-b.d*.62);}

// Extend collision with interior furniture, props and parked cars.
const blockedBeforePhase39=blockedPosition;
blockedPosition=function(x,z){
  if(blockedBeforePhase39(x,z))return true;
  for(const c of interiorCols39){if(!c.root.visible||!c.root.parent||!c.root.parent.visible)continue;if(c.owner&&c.owner.userData.break39&&c.owner.userData.break39.broken)continue;if(Math.abs(x-c.x)<c.w/2+.18&&Math.abs(z-c.z)<c.d/2+.18)return true;}
  for(const c of propCols39){if(!c.g.visible)continue;const p=c.g.position;if(Math.abs(x-p.x)<c.w/2+.2&&Math.abs(z-p.z)<c.d/2+.2)return true;}
  for(const car of cars19){if(car===activeCar19)continue;const dx=x-car.group.position.x,dz=z-car.group.position.z,a=-car.group.rotation.y,ca=Math.cos(a),sa=Math.sin(a),lx=dx*ca-dz*sa,lz=dx*sa+dz*ca;if(Math.abs(lx)<1.7&&Math.abs(lz)<2.75)return true;}
  return false;
};

// ---------- Pooled material-aware combat VFX ----------
const pCount39=phaseAllDesktop39?130:64,pPos39=new Float32Array(pCount39*3),pCol39=new Float32Array(pCount39*3),pLife39=new Float32Array(pCount39),pVel39=Array.from({length:pCount39},()=>new THREE.Vector3());for(let i=0;i<pCount39;i++)pPos39[i*3+1]=-999;
const pGeo39=new THREE.BufferGeometry();pGeo39.setAttribute('position',new THREE.BufferAttribute(pPos39,3));pGeo39.setAttribute('color',new THREE.BufferAttribute(pCol39,3));const pPts39=new THREE.Points(pGeo39,new THREE.PointsMaterial({size:phaseAllDesktop39?0.075:0.06,vertexColors:true,transparent:true,opacity:.92,depthWrite:false}));scene.add(pPts39);let pCursor39=0;
function particles39(point,color,count,power,normal){const n=normal?normal.clone().normalize():new THREE.Vector3(0,1,0),cc=new THREE.Color(color),realCount=Math.max(1,Math.round(count*fxBudget39));for(let q=0;q<realCount;q++){const i=pCursor39++%pCount39,v=new THREE.Vector3(rand(-1,1),rand(-.1,1.2),rand(-1,1)).normalize();pPos39[i*3]=point.x+n.x*.04;pPos39[i*3+1]=point.y+n.y*.04;pPos39[i*3+2]=point.z+n.z*.04;pVel39[i].copy(n).multiplyScalar(rand(.3,1.1)*power).addScaledVector(v,rand(.2,1)*power);pLife39[i]=rand(.18,.52);pCol39[i*3]=cc.r;pCol39[i*3+1]=cc.g;pCol39[i*3+2]=cc.b;}pGeo39.attributes.position.needsUpdate=true;pGeo39.attributes.color.needsUpdate=true;}
const decals39=[];for(let i=0;i<(phaseAllDesktop39?40:20);i++){const d=new THREE.Mesh(new THREE.CircleGeometry(.055,8),new THREE.MeshBasicMaterial({color:0x20201e,transparent:true,opacity:.48,depthWrite:false,polygonOffset:true,polygonOffsetFactor:-2,polygonOffsetUnits:-2}));d.visible=false;scene.add(d);decals39.push(d);}let decalIndex39=0;
function decal39(hit){if(!hit||!hit.face)return;const d=decals39[decalIndex39++%decals39.length],n=hit.face.normal.clone();n.applyMatrix3(new THREE.Matrix3().getNormalMatrix(hit.object.matrixWorld)).normalize();d.position.copy(hit.point).addScaledVector(n,.012);d.quaternion.setFromUnitVectors(new THREE.Vector3(0,0,1),n);d.visible=true;d.userData.until39=performance.now()+8500;}
function carFrom39(o){let p=o;for(let i=0;i<8&&p;i++,p=p.parent){if(p.userData&&p.userData.car19)return p.userData.car19;}return null;}
function surface39(hit){const o=hit.object;if(o.userData&&o.userData.surface39)return o.userData.surface39;if(o.userData&&o.userData.resource&&o.userData.resource.kind==='tree')return'wood';if(carFrom39(o))return'metal';const m=Array.isArray(o.material)?o.material[0]:o.material;if(m&&(m.metalness||0)>.38)return'metal';if(o===terrain)return'dirt';return'concrete';}
function destroy39(o,hit){const b=o.userData?o.userData.break39:null;if(!b||b.broken)return;b.hp-=b.type==='glass'?24:20;if(b.hp>0)return;b.broken=true;o.visible=false;syncBlocker39(o,false);const color=b.type==='glass'?0xb9e2f3:b.type==='wood'?0xa97b4b:0xffb24c;particles39(hit.point,color,b.type==='glass'?14:9,1.2,hit.face?hit.face.normal:null);impactSound39(b.type,true);}
let rayHit39=null;const nearestBefore39=nearestHit;nearestHit=function(dir,maxDist){const h=nearestBefore39(dir,maxDist);rayHit39=h;return h;};
const impactBefore39=impact;impact=function(point,color){const hit=rayHit39;rayHit39=null;impactBefore39(point,color);if(!hit||!hit.object)return;const s=surface39(hit);let c=0xb6aea3,power=.8;if(s==='metal'){c=0xffbf5b;power=1.3;}else if(s==='wood')c=0xaa7a49;else if(s==='glass'){c=0xb9e2f3;power=1.2;}else if(s==='dirt')c=0x8d7758;particles39(point,c,phaseAllDesktop39?8:4,power,hit.face?hit.face.normal:null);if(s!=='glass'&&s!=='dirt')decal39(hit);destroy39(hit.object,hit);impactSound39(s,false);const car=carFrom39(hit.object);if(car)damageCar39(car,20,point);};

// ---------- Car bullet damage / wrecks ----------
const proxyMat39=new THREE.MeshBasicMaterial({transparent:true,opacity:0,depthWrite:false,colorWrite:false});const carSmoke39=[];
for(const car of cars19){car.hp39=260;car.wrecked39=false;const proxy=new THREE.Mesh(new THREE.BoxGeometry(3.3,1.7,5.55),proxyMat39);proxy.position.set(0,1.05,0);proxy.userData.surface39='metal';proxy.userData.carProxy39=car;car.group.add(proxy);car.proxy39=proxy;syncBlocker39(proxy,true);}
function smokeCar39(car){if(car.smoke39)return;const g=new THREE.Group();g.position.set(0,1.45,-1.25);car.group.add(g);car.smoke39=g;for(let i=0;i<4;i++){const s=mesh39(g,new THREE.SphereGeometry(.14+Math.random()*.1,7,6),new THREE.MeshBasicMaterial({color:0x4a4b49,transparent:true,opacity:.15,depthWrite:false}),rand(-.2,.2),rand(0,.3),rand(-.2,.2),false);s.userData.phase39=Math.random()*6.28;carSmoke39.push(s);}}
function damageCar39(car,amount,point){if(!car||car.wrecked39)return;car.hp39=Math.max(0,car.hp39-amount);if(car.hp39<150)smokeCar39(car);if(car.hp39===0){car.wrecked39=true;car.speed=0;car.maxSpeed=0;particles39(point||car.group.position,0xff9445,20,2.2,new THREE.Vector3(0,1,0));explosionSound39();car.group.traverse(function(o){if(o.isMesh&&o.material&&o.material.color&&o!==car.proxy39){o.material=o.material.clone();o.material.color.multiplyScalar(.52);if('roughness'in o.material)o.material.roughness=Math.max(.72,o.material.roughness);}});}}
const enterBefore39=enterCar19;enterCar19=function(car){if(car&&car.wrecked39){message('VEHICLE WRECKED',1.4);return;}enterBefore39(car);};

// ---------- Richer procedural sound ----------
let ambSrc39=null,ambGain39=null,engOsc39=null,engGain39=null,lastImpact39=0;
function audio39(){try{if(!audioCtx)audioCtx=new(window.AudioContext||window.webkitAudioContext)();if(audioCtx.state==='suspended'&&audioCtx.resume)audioCtx.resume();if(!ambSrc39){const len=(audioCtx.sampleRate*.7)|0,b=audioCtx.createBuffer(1,len,audioCtx.sampleRate),d=b.getChannelData(0);for(let i=0;i<len;i++)d[i]=Math.random()*2-1;ambSrc39=audioCtx.createBufferSource();ambSrc39.buffer=b;ambSrc39.loop=true;const f=audioCtx.createBiquadFilter();f.type='lowpass';f.frequency.value=430;ambGain39=audioCtx.createGain();ambGain39.gain.value=.0025;ambSrc39.connect(f);f.connect(ambGain39);ambGain39.connect(audioCtx.destination);ambSrc39.start();}if(!engOsc39){engOsc39=audioCtx.createOscillator();engOsc39.type='sawtooth';engGain39=audioCtx.createGain();engGain39.gain.value=.0001;const f=audioCtx.createBiquadFilter();f.type='lowpass';f.frequency.value=250;engOsc39.connect(f);f.connect(engGain39);engGain39.connect(audioCtx.destination);engOsc39.start();}return audioCtx;}catch(e){return null;}}
function noise39(dur,vol,hp){const ctx=audio39();if(!ctx)return;const len=Math.max(1,(ctx.sampleRate*dur)|0),b=ctx.createBuffer(1,len,ctx.sampleRate),d=b.getChannelData(0);for(let i=0;i<len;i++)d[i]=(Math.random()*2-1)*(1-i/len);const s=ctx.createBufferSource(),f=ctx.createBiquadFilter(),g=ctx.createGain();s.buffer=b;f.type='highpass';f.frequency.value=hp;g.gain.value=vol;s.connect(f);f.connect(g);g.connect(ctx.destination);s.start();}
function impactSound39(type,broken){const now=performance.now();if(!broken&&now-lastImpact39<45)return;lastImpact39=now;if(type==='glass')noise39(.05,broken?.034:.016,2100);else if(type==='metal')noise39(.025,broken?.024:.011,1500);else if(type==='wood')noise39(.03,broken?.020:.009,700);else noise39(.02,.006,380);}
function explosionSound39(){noise39(.18,.055,70);tone(44,.20,.055,'sine');}
function gunExtra39(w){const f=weaponFamily27(w);if(f==='shotgun')noise39(.065,.044,130);else if(f==='pistol'||f==='revolver')noise39(.038,.027,520);else if(f==='smg')noise39(.022,.019,720);else noise39(.032,.024,440);}
const shootBeforeSound39=shootGun;shootGun=function(w){const before=w?w.mag:null;const r=shootBeforeSound39(w);if(w&&w.type==='gun'&&before>w.mag)gunExtra39(w);return r;};
let footPrev39=player.pos.clone(),foot39=0;const playerBeforeSound39=updatePlayer;updatePlayer=function(dt){const ox=footPrev39.x,oz=footPrev39.z;playerBeforeSound39(dt);const moved=Math.hypot(player.pos.x-ox,player.pos.z-oz);footPrev39.copy(player.pos);if(!activeCar19&&!player.dead){foot39+=moved;if(foot39>2.2){foot39=0;noise39(.016,.0048,650);}}if(audioCtx&&engOsc39&&engGain39){const speed=activeCar19?Math.abs(activeCar19.speed||0):0;let target=.0001;if(activeCar19&&!activeCar19.wrecked39)target=Math.min(.019,.005+speed*.0005);engOsc39.frequency.setTargetAtTime(48+speed*5.4,audioCtx.currentTime,.07);engGain39.gain.setTargetAtTime(target,audioCtx.currentTime,.09);}};

// ---------- Biome atmosphere ----------
const aCount39=phaseAllDesktop39?125:60,aPos39=new Float32Array(aCount39*3),aVel39=new Float32Array(aCount39*3);for(let i=0;i<aCount39;i++){aPos39[i*3]=rand(-28,28);aPos39[i*3+1]=rand(.2,12);aPos39[i*3+2]=rand(-28,28);aVel39[i*3]=rand(-.12,.12);aVel39[i*3+1]=rand(-.06,.04);aVel39[i*3+2]=rand(-.1,.1);}
const aGeo39=new THREE.BufferGeometry();aGeo39.setAttribute('position',new THREE.BufferAttribute(aPos39,3));const aMat39=new THREE.PointsMaterial({color:0xd9d2ba,size:.08,transparent:true,opacity:.18,depthWrite:false});const atmosphere39=new THREE.Points(aGeo39,aMat39);scene.add(atmosphere39);
const mist39=[];const mistCount39=phaseAllDesktop39?7:4;for(let i=0;i<mistCount39;i++){const m=new THREE.Mesh(new THREE.CircleGeometry(rand(4,8),18),new THREE.MeshBasicMaterial({color:0xb8c5c1,transparent:true,opacity:.033,depthWrite:false,side:THREE.DoubleSide}));m.rotation.x=-Math.PI/2;m.visible=false;m.userData.a39=i/mistCount39*Math.PI*2;m.userData.r39=rand(7,18);scene.add(m);mist39.push(m);}
const glare39=document.createElement('div');glare39.style.cssText='position:fixed;inset:0;z-index:4;pointer-events:none;background:radial-gradient(circle at 17% 14%,rgba(255,226,172,.13),rgba(255,226,172,.035) 12%,transparent 28%)';document.body.appendChild(glare39);
function atmosphereUpdate39(dt){const b=biomeAt(player.pos.x,player.pos.z),alpine=b===BIOMES.alpine,bad=b===BIOMES.badlands,marsh=b===BIOMES.marsh;atmosphere39.position.set(player.pos.x,groundY(player.pos.x,player.pos.z),player.pos.z);aMat39.color.setHex(alpine?0xf4f6f5:bad?0xcaa87b:marsh?0xd3ded9:0xd9d2ba);if(alpine)aMat39.opacity=.38;else if(bad)aMat39.opacity=.18;else if(marsh)aMat39.opacity=.13;else aMat39.opacity=.08;const p=aGeo39.attributes.position.array;for(let i=0;i<aCount39;i++){p[i*3]+=aVel39[i*3]*dt*4;p[i*3+1]+=(alpine?-.65:aVel39[i*3+1])*dt;p[i*3+2]+=aVel39[i*3+2]*dt*4;if(Math.abs(p[i*3])>30)p[i*3]*=-.95;if(Math.abs(p[i*3+2])>30)p[i*3+2]*=-.95;if(p[i*3+1]<.1)p[i*3+1]=rand(6,13);}aGeo39.attributes.position.needsUpdate=true;for(const m of mist39){m.visible=marsh&&fxBudget39>.4;if(m.visible){const a=m.userData.a39+performance.now()*.000025,r=m.userData.r39;m.position.set(player.pos.x+Math.cos(a)*r,groundY(player.pos.x,player.pos.z)+.2,player.pos.z+Math.sin(a)*r);}}}

// ---------- Animation polish ----------
const killBefore39=killNpc;killNpc=function(n){const rec=n?n.phaseBRec35:null,alive=!!(n&&!n.dead);killBefore39(n);if(alive&&rec){rec.lArm.rotation.z=-1;rec.rArm.rotation.z=1;rec.lLeg.rotation.x=.4;rec.rLeg.rotation.x=-.35;rec.head.rotation.z=rand(-.35,.35);particles39(n.group.position.clone().add(new THREE.Vector3(0,1.45,0)),0x8d4439,7,.45,new THREE.Vector3(0,1,0));}};

// ---------- Adaptive LOD / performance ----------
let perfFrames39=0,perfStart39=performance.now(),perfTier39=0,lod39=0,cleanup39=0;
function shadow39(g,on){if(!g||g.userData.shadow39===on)return;g.userData.shadow39=on;g.traverse(function(o){if(o.isMesh)o.castShadow=on;});}
function lodUpdate39(){
  if((lod39++%20)!==0)return;const px=player.pos.x,pz=player.pos.z;
  for(const root of interiors39){const b=root.userData.bounds39,d=Math.hypot(b.x-px,b.z-pz),range=phaseAllDesktop39?(perfTier39===0?88:65):52;root.visible=d<range;if(root.userData.light39)root.userData.light39.visible=perfTier39===0&&d<30;root.traverse(function(o){if(o.userData&&o.userData.break39){const active=root.visible&&!o.userData.break39.broken;syncBlocker39(o,active);}});}
  for(const c of propCols39){const p=c.g.position,d=Math.hypot(p.x-px,p.z-pz);c.g.visible=d<(phaseAllDesktop39?135:78);c.g.traverse(function(o){if(o.userData&&o.userData.break39)syncBlocker39(o,c.g.visible&&!o.userData.break39.broken);});}
  for(const n of npcs){if(!n.dead){const d=Math.hypot(n.group.position.x-px,n.group.position.z-pz);shadow39(n.group,d<(phaseAllDesktop39?58:32));}}
  for(const car of cars19){const d=Math.hypot(car.group.position.x-px,car.group.position.z-pz);shadow39(car.group,d<(phaseAllDesktop39?82:44));}
}
function performance39(now){perfFrames39++;if(now-perfStart39>4200){const fps=perfFrames39*1000/(now-perfStart39);perfFrames39=0;perfStart39=now;if(fps<30){perfTier39=2;fxBudget39=.3;}else if(fps<40){perfTier39=1;fxBudget39=.55;}else{perfTier39=0;fxBudget39=phaseAllDesktop39?1:.58;}atmosphere39.visible=perfTier39<2;glare39.style.opacity=perfTier39===2?'.55':'1';}requestAnimationFrame(performance39);}requestAnimationFrame(performance39);

const npcBeforeAll39=updateNPCs;updateNPCs=function(dt){npcBeforeAll39(dt);const pp=pGeo39.attributes.position.array;for(let i=0;i<pCount39;i++){if(pLife39[i]>0){pLife39[i]-=dt;pVel39[i].y-=4.8*dt;pp[i*3]+=pVel39[i].x*dt;pp[i*3+1]+=pVel39[i].y*dt;pp[i*3+2]+=pVel39[i].z*dt;if(pLife39[i]<=0)pp[i*3+1]=-999;}}pGeo39.attributes.position.needsUpdate=true;const now=performance.now();for(const d of decals39)if(d.visible&&now>(d.userData.until39||0))d.visible=false;for(const s of carSmoke39){const t=now*.001+s.userData.phase39;s.position.y=.15+((t*.28)%1.15);s.position.x=Math.sin(t*1.6)*.15;s.material.opacity=Math.max(0,.16*(1-(s.position.y-.15)/1.15));}atmosphereUpdate39(dt);lodUpdate39();cleanup39+=dt;if(cleanup39>2.5){cleanup39=0;const seen=new Set();for(let i=blockers.length-1;i>=0;i--){const o=blockers[i];if(!o||!o.parent||seen.has(o)||(o.userData&&o.userData.break39&&o.userData.break39.broken)){blockers.splice(i,1);continue;}seen.add(o);}}};

message('ALL GRAPHICS PHASES COMPLETE · interiors, destruction, atmosphere, sound + LOD',4.5);
