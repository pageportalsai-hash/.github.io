// FRONTIER ZERO COMBAT POLISH 65C — recoil, weapon audio, final frame, QA
const recoilProfiles65={sniper:{pitch:.115,yaw:.018,kick:.115,roll:.028},shotgun:{pitch:.095,yaw:.024,kick:.105,roll:.040},rifle:{pitch:.046,yaw:.012,kick:.060,roll:.018},dmr:{pitch:.065,yaw:.014,kick:.072,roll:.022},smg:{pitch:.028,yaw:.010,kick:.044,roll:.014},pistol:{pitch:.055,yaw:.020,kick:.058,roll:.030},revolver:{pitch:.082,yaw:.024,kick:.078,roll:.036}};
let recoilPitch65=0,recoilYaw65=0,recoilKick65=0,recoilRoll65=0;
const weaponBaseZ65=typeof weaponModels27!=='undefined'?weaponModels27.position.z:0,weaponBaseXRot65=typeof weaponModels27!=='undefined'?weaponModels27.rotation.x:0;
let noiseBuffer65=null,noiseRate65=0;
function ensureAudio65(){try{audioCtx ||= new (window.AudioContext||window.webkitAudioContext)();if(audioCtx.state==='suspended')audioCtx.resume();return audioCtx;}catch{return null;}}
function noise65(ctx){if(noiseBuffer65&&noiseRate65===ctx.sampleRate)return noiseBuffer65;noiseRate65=ctx.sampleRate;const len=Math.floor(ctx.sampleRate*.22),b=ctx.createBuffer(1,len,ctx.sampleRate),a=b.getChannelData(0);for(let i=0;i<len;i++){const fade=1-i/len;a[i]=(Math.random()*2-1)*fade;}noiseBuffer65=b;return b;}
function soundProfile65(fam){if(fam==='sniper')return{bass:72,crack:1850,dur:.16,vol:.060,e1:.18,e2:.38};if(fam==='shotgun')return{bass:88,crack:1120,dur:.14,vol:.062,e1:.19,e2:.40};if(fam==='smg')return{bass:155,crack:2350,dur:.055,vol:.036,e1:.105,e2:.23};if(fam==='pistol')return{bass:175,crack:2150,dur:.075,vol:.044,e1:.13,e2:.27};if(fam==='revolver')return{bass:132,crack:1950,dur:.105,vol:.052,e1:.15,e2:.31};if(fam==='dmr')return{bass:102,crack:2100,dur:.10,vol:.050,e1:.15,e2:.32};return{bass:125,crack:2250,dur:.078,vol:.043,e1:.125,e2:.27};}
function gunLayer65(ctx,p,delay=0,scale=1,echo=false){const t=ctx.currentTime+delay;const osc=ctx.createOscillator(),og=ctx.createGain();osc.type=echo?'sine':'triangle';osc.frequency.setValueAtTime(p.bass*(echo?.72:1),t);osc.frequency.exponentialRampToValueAtTime(Math.max(38,p.bass*.62),t+p.dur);og.gain.setValueAtTime(.0001,t);og.gain.exponentialRampToValueAtTime(p.vol*scale,t+.004);og.gain.exponentialRampToValueAtTime(.0001,t+p.dur*(echo?1.8:1));osc.connect(og);og.connect(ctx.destination);osc.start(t);osc.stop(t+p.dur*(echo?1.9:1)+.02);const src=ctx.createBufferSource(),filter=ctx.createBiquadFilter(),ng=ctx.createGain();src.buffer=noise65(ctx);filter.type=echo?'lowpass':'bandpass';filter.frequency.value=echo?720:p.crack;filter.Q.value=echo?.5:1.2;ng.gain.setValueAtTime(.0001,t);ng.gain.exponentialRampToValueAtTime(p.vol*(echo?.22:.72)*scale,t+.002);ng.gain.exponentialRampToValueAtTime(.0001,t+(echo?.13:.065));src.connect(filter);filter.connect(ng);ng.connect(ctx.destination);src.start(t);src.stop(t+.19);}
function playGunSound65(w){const ctx=ensureAudio65();if(!ctx)return;const fam=weaponFamily27(w),p=soundProfile65(fam);gunLayer65(ctx,p,0,1,false);gunLayer65(ctx,p,p.e1,.34,true);gunLayer65(ctx,p,p.e2,.17,true);}
const shootGunBeforeCombat65=shootGun;
shootGun=function(w){const before=w?.mag;activeShotWeapon65=w;baseShotDamage65=w?.damage||0;shotHadHeadshot65=false;shotHitNpc65=false;let out;try{out=shootGunBeforeCombat65(w);}finally{if(w)w.damage=baseShotDamage65;activeShotWeapon65=null;}if(w?.type==='gun'&&Number.isFinite(before)&&w.mag<before){const fam=weaponFamily27(w),rp=recoilProfiles65[fam]||recoilProfiles65.rifle;recoilPitch65=Math.min(.16,recoilPitch65+rp.pitch);recoilYaw65+=rand(-rp.yaw,rp.yaw);recoilKick65=Math.min(.16,recoilKick65+rp.kick);recoilRoll65+=rand(-rp.roll,rp.roll);playGunSound65(w);if(shotHadHeadshot65)headshotHud65.textContent='HEADSHOT';}return out;};
const updatePlayerBeforeCombat65=updatePlayer;
updatePlayer=function(dt){const out=updatePlayerBeforeCombat65(dt);const decay=Math.exp(-dt*13);recoilPitch65*=decay;recoilYaw65*=Math.exp(-dt*15);recoilKick65*=Math.exp(-dt*18);recoilRoll65*=Math.exp(-dt*14);const firstPerson=!(typeof thirdPerson16!=='undefined'&&thirdPerson16)&&!(typeof activeEmote61!=='undefined'&&activeEmote61);if(firstPerson&&typeof weaponModels27!=='undefined'){weaponModels27.position.z=weaponBaseZ65+recoilKick65;weaponModels27.rotation.x=weaponBaseXRot65-recoilPitch65*.9;weaponModels27.rotation.z+=recoilRoll65*.6;camera.rotation.x-=recoilPitch65*.23;camera.rotation.y+=recoilYaw65*.14;}if(typeof casings35!=='undefined')for(const c of casings35){if(c.life<=0||!c.m.visible)continue;const gy=groundY(c.m.position.x,c.m.position.z)+.035;if(c.m.position.y<=gy&&c.v.y<0&&c.bounces65<2){c.m.position.y=gy;c.v.y=Math.abs(c.v.y)*(c.bounces65===0?.34:.18);c.v.x*=.68;c.v.z*=.68;c.spin.multiplyScalar(.65);c.bounces65++;}}for(const p of fxPool65){if(p.life<=0)continue;p.life-=dt;p.v.y-=p.gravity*dt;p.m.position.addScaledVector(p.v,dt);p.m.material.opacity=Math.max(0,p.life/p.max);if(p.life<=0)p.m.visible=false;}for(const d of decalPool65){if(d.life<=0)continue;d.life-=dt;d.m.material.opacity=Math.min(.5,Math.max(0,d.life<1?d.life*.5:.5));if(d.life<=0)d.m.visible=false;}return out;};
const updateNPCsBeforeCombat65=updateNPCs;
updateNPCs=function(dt){const out=updateNPCsBeforeCombat65(dt),now=performance.now();for(const n of npcs){if(!n?.group)continue;const death=deathState65.get(n);if(n.dead&&death&&n.group.parent){const q=clamp((now-death.start)/death.duration,0,1),e=1-Math.pow(1-q,3);n.group.rotation.z=death.side*(1.47*e);n.group.rotation.x=-.16*Math.sin(Math.PI*q);n.group.position.y=death.baseY-.035*e;const rec=n.phaseBRec35;if(rec?.torso){rec.torso.rotation.x=-.18*e;rec.head.rotation.x=-.10*e;}continue;}if(n.dead)continue;const s=staggerState65.get(n);if(!s||s.life<=0)continue;s.life-=dt;const k=Math.max(0,s.life/s.max),pulse=Math.sin((1-k)*Math.PI);const rec=n.phaseBRec35;if(rec?.torso){rec.torso.rotation.x+=.12*pulse;rec.torso.rotation.z+=s.side*(s.head?.16:.10)*pulse;}if(rec?.head)rec.head.rotation.x+=(s.head?-.22:.07)*pulse;if(rec?.lArm)rec.lArm.rotation.x+=.10*pulse;if(rec?.rArm)rec.rArm.rotation.x+=.10*pulse;if(s.life>s.max*.55){const nx=n.group.position.x+s.away.x*dt*(s.head?.85:.45),nz=n.group.position.z+s.away.z*dt*(s.head?.85:.45);if(!blockedPosition(nx,nz)){n.group.position.x=nx;n.group.position.z=nz;n.group.position.y=groundY(nx,nz);}}}return out;};
function qaPassCombat65(name,checks){const r={name,passed:0,total:checks.length,issues:[]};for(const [label,fn] of checks){let ok=false;try{ok=!!fn();}catch(e){r.issues.push(label+': '+e.message);continue;}if(ok)r.passed++;else r.issues.push(label);}return r;}
window.__frontierZeroCombatQA65={gameplay:qaPassCombat65('combat-gameplay',[['headshot multiplier',()=>typeof isHeadshot65==='function'&&1.85>1],['stagger state',()=>typeof triggerStagger65==='function'],['death animation state',()=>typeof deathState65!=='undefined'],['kill progression retained',()=>typeof playerKills24!=='undefined'&&typeof killNpc==='function']]),visual:qaPassCombat65('combat-visual',[['hit marker',()=>!!hitMarker65?.parentElement],['impact particle pool',()=>fxPool65.length===64],['bullet decal pool',()=>decalPool65.length===28],['shell casing pool',()=>typeof casings35!=='undefined'&&casings35.length>=32]]),stability:qaPassCombat65('combat-stability',[['weapon models retained',()=>typeof weaponModels27!=='undefined'],['tactical AI retained',()=>typeof weaponRole64==='function'],['Griddy retained',()=>typeof applyRealGriddy62==='function'],['NPC LOD retained',()=>typeof setNpcRigLod43==='function']])};
window.__frontierZeroCombat65={loaded:true,headshotMultiplier:1.85,features:['headshot indicator','fall animations','NPC stagger','impact decals','hit particles','bouncing shell casings','weapon recoil','weapon-family sounds','distant echoes']};
if(typeof graphicsBadge21!=='undefined'){graphicsBadge21.style.opacity='1';graphicsBadge21.textContent='COMBAT POLISH 65 · HEADSHOTS · RECOIL · IMPACT FX · ECHO';setTimeout(()=>graphicsBadge21.style.opacity='0',8000);}message('COMBAT POLISH LIVE · HEADSHOTS · STAGGER · DEATH FX · CASINGS · RECOIL · GUN AUDIO',5.0);
