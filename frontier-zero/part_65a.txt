// FRONTIER ZERO COMBAT FEEDBACK 65A — critical hit marker, stagger, surface impacts
const combatDesktop65=matchMedia('(pointer:fine)').matches;
const hitMarker65=document.createElement('div');hitMarker65.style.cssText='position:fixed;left:50%;top:50%;width:34px;height:34px;transform:translate(-50%,-50%);z-index:82;pointer-events:none;opacity:0;transition:opacity .08s';
for(const [x,y,r] of [[6,6,45],[28,6,135],[6,28,-45],[28,28,-135]]){const l=document.createElement('div');l.style.cssText='position:absolute;width:9px;height:2px;background:#fff;border-radius:2px;box-shadow:0 1px 2px #000;transform-origin:center';l.style.left=x+'px';l.style.top=y+'px';l.style.transform=`rotate(${r}deg)`;hitMarker65.appendChild(l);}document.body.appendChild(hitMarker65);
const headshotHud65=document.createElement('div');headshotHud65.style.cssText='position:fixed;left:50%;top:calc(50% + 31px);transform:translateX(-50%);z-index:83;pointer-events:none;opacity:0;color:#ffd55f;font:1000 12px system-ui;letter-spacing:.09em;text-shadow:0 2px 4px #000;transition:opacity .12s';headshotHud65.textContent='HEADSHOT';document.body.appendChild(headshotHud65);
let hitMarkerTimer65=0,headshotTimer65=0;
function showHitMarker65(head=false){clearTimeout(hitMarkerTimer65);clearTimeout(headshotTimer65);hitMarker65.style.opacity='1';for(const l of hitMarker65.children)l.style.background=head?'#ffd55f':'#fff';if(head){headshotHud65.style.opacity='1';headshotTimer65=setTimeout(()=>headshotHud65.style.opacity='0',620);}hitMarkerTimer65=setTimeout(()=>hitMarker65.style.opacity='0',120);if(head)tone(870,.045,.022,'sine');}
let activeShotWeapon65=null,baseShotDamage65=0,lastImpactKind65='dust';let lastImpactNormal65=new THREE.Vector3(0,1,0),shotHadHeadshot65=false,shotHitNpc65=false;const staggerState65=new WeakMap();
function npcFromHit65(hit){let o=hit?.object;for(let i=0;i<5&&o;i++,o=o.parent){if(o.userData?.npc)return o.userData.npc;}return null;}
function isHeadshot65(hit,n){if(!hit||!n?.group)return false;if(n.hitMeshes?.[1]===hit.object)return true;return hit.point.y-n.group.position.y>1.82;}
function hitNormal65(hit){if(hit?.face?.normal&&hit.object?.matrixWorld){try{return hit.face.normal.clone().transformDirection(hit.object.matrixWorld).normalize();}catch{}}return new THREE.Vector3(0,1,0);}
function classifyImpact65(hit,n){if(n)return 'character';if(hit?.object===terrain)return 'dirt';const m=Array.isArray(hit?.object?.material)?hit.object.material[0]:hit?.object?.material;if((m?.metalness??0)>.30)return 'metal';return 'dust';}
function triggerStagger65(n,head,point){if(!n||n.dead)return;const away=n.group.position.clone().sub(player.pos);away.y=0;if(away.lengthSq()<.001)away.set(rand(-1,1),0,rand(-1,1));away.normalize();staggerState65.set(n,{life:head?.34:.24,max:head?.34:.24,head,away,side:Math.random()<.5?-1:1,point:point?.clone?.()});}
const nearestHitBeforeCombat65=nearestHit;
nearestHit=function(dir,maxDist=130){if(activeShotWeapon65)activeShotWeapon65.damage=baseShotDamage65;const hit=nearestHitBeforeCombat65(dir,maxDist);if(activeShotWeapon65){if(hit){const n=npcFromHit65(hit),head=n&&isHeadshot65(hit,n);lastImpactKind65=classifyImpact65(hit,n);lastImpactNormal65=hitNormal65(hit);if(n&&!n.dead){shotHitNpc65=true;showHitMarker65(!!head);triggerStagger65(n,!!head,hit.point);if(head){shotHadHeadshot65=true;activeShotWeapon65.damage=baseShotDamage65*1.85;}}}else{lastImpactKind65='dust';lastImpactNormal65.set(0,1,0);}}return hit;};
const fxGeo65=new THREE.SphereGeometry(.018,5,4),fxPool65=[];for(let i=0;i<64;i++){const m=new THREE.Mesh(fxGeo65,new THREE.MeshBasicMaterial({color:0xc7b69b,transparent:true,opacity:0,depthWrite:false}));m.visible=false;scene.add(m);fxPool65.push({m,v:new THREE.Vector3(),life:0,max:0,gravity:3.5});}let fxCursor65=0;
const decalGeo65=new THREE.CircleGeometry(.045,9),decalPool65=[];for(let i=0;i<28;i++){const m=new THREE.Mesh(decalGeo65,new THREE.MeshBasicMaterial({color:0x242424,transparent:true,opacity:0,depthWrite:false,polygonOffset:true,polygonOffsetFactor:-2}));m.visible=false;scene.add(m);decalPool65.push({m,life:0,max:0});}let decalCursor65=0;const zAxis65=new THREE.Vector3(0,0,1);
function spawnParticle65(point,color,vel,life=.28,scale=1,gravity=3.5){const p=fxPool65[fxCursor65++%fxPool65.length];p.m.position.copy(point);p.m.material.color.setHex(color);p.m.material.opacity=1;p.m.scale.setScalar(scale);p.m.visible=true;p.v.copy(vel);p.life=p.max=life;p.gravity=gravity;}
function spawnDecal65(point,normal,kind){if(kind==='character')return;const d=decalPool65[decalCursor65++%decalPool65.length];d.m.position.copy(point).addScaledVector(normal,.008);d.m.quaternion.setFromUnitVectors(zAxis65,normal);d.m.material.color.setHex(kind==='metal'?0x333536:0x403c36);d.m.material.opacity=.50;d.m.scale.setScalar(kind==='metal'?.72:1);d.m.visible=true;d.life=d.max=6.5;}
function enhancedImpact65(point,kind,normal){if(kind==='character'){for(let i=0;i<3;i++)spawnParticle65(point,i===0?0x8f3028:0x69241f,new THREE.Vector3(rand(-.55,.55),rand(.15,.85),rand(-.55,.55)),rand(.20,.34),rand(.75,1.15),3.8);return;}spawnDecal65(point,normal,kind);if(kind==='metal'){for(let i=0;i<3;i++)spawnParticle65(point,0xffc86c,normal.clone().multiplyScalar(rand(.6,1.4)).add(new THREE.Vector3(rand(-.7,.7),rand(-.25,.75),rand(-.7,.7))),rand(.12,.22),rand(.6,.95),1.5);}else{const color=kind==='dirt'?0x9b8669:0xa7a59e;for(let i=0;i<4;i++)spawnParticle65(point,color,new THREE.Vector3(rand(-.45,.45),rand(.15,.75),rand(-.45,.45)),rand(.22,.40),rand(.8,1.3),2.6);}}
const impactBeforeCombat65=impact;impact=function(point,color){const out=impactBeforeCombat65(point,color);if(activeShotWeapon65)enhancedImpact65(point,lastImpactKind65,lastImpactNormal65);return out;};
